# Cobri frontend

Reserved for a React and TypeScript frontend, to be implemented by a coding agent later. Day 1 includes no screens, package manifest, dependencies, or frontend startup command.

The API keeps `ui_locale` and `instructional_language` separate. Preserve that distinction when implementing the learning flow.
